# CipherScan local application stack: zebrad + Postgres + Redis
# + API + web. The production Rust indexer lives in the adjacent
# cipherscan-rust repository; its committed schema and migrations are mounted
# here so local services use the same database contract as CI/production.
#
# Image versions are pinned via variables so you can point any service at a
# specific tag, digest, or commit-tagged image. Copy .env.docker.example to
# .env.docker and run:
#
#   docker compose --env-file .env.docker up -d
#
# To pin a service to a specific commit image, set e.g.
#   ZEBRA_IMAGE=zfnd/zebra:sha-<commit>
# Locally built images (web, api) bake the source commit via GIT_COMMIT.

name: zipherscan

services:
  # ----- Zebra full node (JSON-RPC for the API) -----
  zebrad:
    image: ${ZEBRA_IMAGE:-zfnd/zebra:2.4.0}
    restart: unless-stopped
    environment:
      ZEBRA_NETWORK__NETWORK: ${ZEBRA_NETWORK:-Mainnet}
      ZEBRA_RPC__LISTEN_ADDR: 0.0.0.0:8232
      # Internal-only network; cookie auth is disabled so peers can reach RPC.
      ZEBRA_RPC__ENABLE_COOKIE_AUTH: "false"
      ZEBRA_STATE__CACHE_DIR: /var/cache/zebrad
    volumes:
      - zebrad-state:/var/cache/zebrad
    expose:
      - "8232"
    networks: [cipherscan]

  # ----- PostgreSQL (explorer database) -----
  postgres:
    image: ${POSTGRES_IMAGE:-postgres:16-bookworm}
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_ADMIN_PASSWORD:-postgres-local-only}
      POSTGRES_DB: ${DB_NAME:-zcash_explorer}
      APP_DB_USER: ${DB_USER:-zcash_user}
      APP_DB_PASSWORD: ${DB_PASSWORD:-changeme}
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./docker/postgres/001-create-app-role.sh:/docker-entrypoint-initdb.d/001-create-app-role.sh:ro
      - ${CIPHERSCAN_RUST_DIR:-../cipherscan-rust}/schema/postgres.sql:/docker-entrypoint-initdb.d/002-cipherscan-schema.sql:ro
    expose:
      - "5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-zcash_user} -d ${DB_NAME:-zcash_explorer}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks: [cipherscan]

  # Apply migrations after both a fresh snapshot restore and existing-volume
  # startup. The migration runner is idempotent and records applied versions.
  migrate:
    image: ${POSTGRES_IMAGE:-postgres:16-bookworm}
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PGHOST: postgres
      PGPORT: "5432"
      PGDATABASE: ${DB_NAME:-zcash_explorer}
      PGUSER: postgres
      PGPASSWORD: ${POSTGRES_ADMIN_PASSWORD:-postgres-local-only}
    volumes:
      - ${CIPHERSCAN_RUST_DIR:-../cipherscan-rust}:/workspace/cipherscan-rust:ro
    command:
      - bash
      - -ceu
      - |
        export DATABASE_URL="postgresql://$${PGUSER}@$${PGHOST}:$${PGPORT}/$${PGDATABASE}"
        bash /workspace/cipherscan-rust/deploy/apply-migrations.sh
    networks: [cipherscan]

  # ----- Redis (API cache + WebSocket pub/sub) -----
  redis:
    image: ${REDIS_IMAGE:-redis:7-bookworm}
    restart: unless-stopped
    command: ["redis-server", "--save", "60", "1", "--loglevel", "warning"]
    volumes:
      - redis-data:/data
    expose:
      - "6379"
    networks: [cipherscan]

  # ----- Express API backend -----
  api:
    image: ${API_IMAGE:-cipherscan-api:local}
    build:
      context: ./server/api
      args:
        NODE_IMAGE: ${NODE_IMAGE:-node:22.14.0-bookworm-slim}
        GIT_COMMIT: ${GIT_COMMIT:-unknown}
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_started
      zebrad:
        condition: service_started
    environment:
      NODE_ENV: production
      PORT: "3001"
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_NAME: ${DB_NAME:-zcash_explorer}
      DB_USER: ${DB_USER:-zcash_user}
      DB_PASSWORD: ${DB_PASSWORD:-changeme}
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      API_LIST_CACHE_ENABLED: ${API_LIST_CACHE_ENABLED:-0}
      API_CACHE_NAMESPACE: ${API_CACHE_NAMESPACE:-mainnet}
      API_LIST_CACHE_MAX_ENTRIES: ${API_LIST_CACHE_MAX_ENTRIES:-1000}
      API_LIST_CACHE_REDIS_TIMEOUT_MS: ${API_LIST_CACHE_REDIS_TIMEOUT_MS:-50}
      ZEBRA_RPC_URL: http://zebrad:8232
      ZEBRA_GRPC_URL: ""
      COMPACT_BLOCK_CACHE_DIR: /var/cache/cipherscan/compact-blocks
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:3000}
    volumes:
      - api-cache:/var/cache/cipherscan
    expose:
      - "3001"
    networks: [cipherscan]

  # ----- Next.js frontend -----
  web:
    image: ${WEB_IMAGE:-cipherscan-web:local}
    build:
      context: .
      args:
        NODE_IMAGE: ${NODE_IMAGE:-node:22.14.0-bookworm-slim}
        GIT_COMMIT: ${GIT_COMMIT:-unknown}
        NEXT_PUBLIC_NETWORK: ${NEXT_PUBLIC_NETWORK:-mainnet}
    restart: unless-stopped
    depends_on:
      - api
      - zebrad
    environment:
      NODE_ENV: production
      PORT: "3000"
      NEXT_PUBLIC_NETWORK: ${NEXT_PUBLIC_NETWORK:-mainnet}
      NEXT_TELEMETRY_DISABLED: "1"
      # Server-side calls inside the compose network:
      ZCASH_RPC_URL: http://zebrad:8232
      CIPHERSCAN_API_URL: http://api:3001
      # Browser-side calls (adjust to your published host):
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:3001}
    ports:
      - "${WEB_PORT:-3000}:3000"
    networks: [cipherscan]

networks:
  cipherscan:
    driver: bridge

volumes:
  zebrad-state:
  postgres-data:
  redis-data:
  api-cache:
