# ML Pattern Detector Dependencies
# Source for requirements.txt. Regenerate with:
#   pip-compile --generate-hashes --allow-unsafe --strip-extras --no-header -o requirements.txt requirements.in
# Install with: pip install --require-hashes -r requirements.txt

# Database
psycopg2-binary>=2.9.12

# ML/Data Science
numpy>=1.24.0
scikit-learn>=1.9.0

# Optional: for future enhancements
# pandas>=2.0.0          # If we need DataFrames
# matplotlib>=3.7.0      # For visualization
# networkx>=3.0          # For graph analysis
